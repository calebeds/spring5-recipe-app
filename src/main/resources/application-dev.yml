spring:
  datasource:
    url: jdbc:mysql://localhost:3306/calebe_dev
    username: calebe_dev_user
    password: calebe
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: calebe_database_create.sql