spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: calebe_prod_user
    password: calebe
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    show-sql: true